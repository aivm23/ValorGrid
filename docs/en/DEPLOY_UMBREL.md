# Umbrel

Umbrel is packaged separately from CasaOS.

Surfaces:

- Official app package under `deploy/umbrel/official/valorgrid/`.
- Community App Store template under `deploy/umbrel/community-store/`.

The package must persist application data and use the Umbrel app proxy. Validate GHCR image availability before publishing an Umbrel release.
