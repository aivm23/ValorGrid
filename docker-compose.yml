services:
  valorgrid:
    build:
      context: .
    image: valorgrid:local
    container_name: valorgrid
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 1325
      PORTFOLIO_DB_PATH: /data/portfolio.sqlite
      VALORGRID_AUTH_USER: valorgrid
      VALORGRID_AUTH_PASSWORD: ""
    ports:
      - "1325:1325"
    volumes:
      - ./data:/data
      - ./backups:/app/.backups
